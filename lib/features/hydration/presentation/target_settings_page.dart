import 'package:flutter/material.dart';

class TargetSettingsPage extends StatelessWidget {
  const TargetSettingsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Hydration Target')),
      body: const SizedBox(),
    );
  }
}
